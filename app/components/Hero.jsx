const Hero = () => {
    return (
        <div name="hero" className="hero-container h-screen w-screen">
            <div className="hero-content bg-[#222] h-full w-full flex flex-col items-center justify-center text-white">
                <h1 className="text-[5rem]">Hero</h1>
            </div>
        </div>
    );
};

export default Hero;
